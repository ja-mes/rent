<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> fade in" style="margin-bottom: 30px">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <%= value %>
  </div>
<% end %>
