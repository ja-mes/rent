<h1 class="display-4 margin-40">Invoice</h1>

<%= render 'shared/errors', obj: @invoice %>

<%= form_for [@customer, @invoice], html: { style: "margin-top: 20px" } do |f| %>
  <div class="form-group margin-20">
    <%= f.date_select :date, order: [:month, :day, :year] %>
  </div>

  <div class="form-group">
    <%= f.label :customer %>
    <%= f.collection_select :customer_id, Customer.where(user: current_user), :id, :full_name, { selected: @customer.id }, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :amount %>
    <div class="input-group">
      <div class="input-group-addon">$</div>
      <%= f.number_field :amount, value: number_with_precision(f.object.amount, precision: 2), step: 0.01, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :memo %>
    <%= f.text_area :memo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-success" %>
    <% unless @invoice.new_record? %>
      <%= link_to 'Delete', customer_invoice_path(@customer, @invoice), method: :delete, class: "btn btn-danger" %>
    <% end %>
  </div>

  <table class="table">
    <thead>
      <tr>
        <th>Account</th>
        <th>Amount</th>
        <!-- <th>Memo</th>
        <th>Property</th>
        <th></th> -->
      </tr>
    </thead>

    <%= f.fields_for :invoice_trans do |ff| %>
      <tr>
        <td>
          <%= ff.number_field :amount, placeholder: "0.00"%>
        </td>

        <td>
          <%= ff.text_field :memo %>
        </td>
      </tr>
    <% end %>
    <!-- <tbody>
      <tr>
        <td>
          <select name="charges[items][][account]">
            <option value="foo">Repairs and Maintenance</option>
            <option value="foo">foo</option>
            <option value="foo">foo</option>
          </select>
        </td>

        <td>
          <input type="number" placeholder="0.00" name="charges[items][][amount]"/>
        </td>

        <td>
          <input type="text" name="charges[items][][memo]"/>
        </td>

        <td>
          <select name="charges[items][][address]">
            <option value="foo">100 Foo St., Blah, AL 35950</option>
            <option value="foo">foo</option>
            <option value="foo">foo</option>
          </select>
        </td>
        <td>
          <a href="#"><i class="fa fa-remove"></i></a>
        </td>

      </tr> 
    </tbody> -->
  </table>
<% end %>


<button class="btn btn-secondary btn-sm"><i class="fa fa-plus"></i></button>


