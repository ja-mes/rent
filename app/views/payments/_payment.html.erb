<tr data-link="<%= edit_customer_payment_path @customer, payment %>">
  <td>Payment</td>
  <td><%= number_with_precision(payment.amount, precision: 2) %></td>
  <td><%= payment.date.strftime("%m/%d/%Y") %></td>
</tr>
