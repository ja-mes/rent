<%= render 'header' %>

<table class="table table-hover row-links">
  <thead class="thead-default">
    <tr>
      <th>Name</th>
      <th>Type</th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |a| %>
      <% if a.name == "Checking" %>
        <tr data-link="<%= check_register_path %>">
      <% else %>
          <tr data-link="<%= account_path a %>">
      <% end %>
        <td><%= a.name %></td>
        <td><%= a.account_type.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
